h1 #{title}
h3.red #{subtitle}
if title == 'Checked-out Loans' || title == 'Loans'
      h4.late-message Return By dates in red are overdue.
p
      a.button(href='/loans/new') Create New Loan
p
      span.inline-option Filters: 
      a.inline-option(href='/loans/all?offset=0&order=#{pagination.order}&search=off') All
      |  
      a.inline-option(href='/loans/overdue?offset=0&order=#{pagination.order}&search=off') Overdue
      |  
      a.inline-option(href='/loans/out?offset=0&order=#{pagination.order}&search=off') Checked Out